"""差分等价 harness——双内核替换的常设安全网。

比对 Python 内核（一等实验内核，参考）与 Rust 内核（生产快路径，opt-in）对
同一 IBCI 语料的数据面（print 输出）——同输入 → 同输出（逐字节等价）。

整个 Rust 内核替换（前端 / 执行核心 / 并发各步落地）的安全网：任何一步的
Rust 内核落地后，经本 harness 验证与 Python 参考内核差分等价（零差异）方可
放行。语料 = 代表语义面种子（算术/控制流/函数/容器/KB 世界模型）+ 后续扩
充（现有测试用例 + fuzz 种子）。
"""
