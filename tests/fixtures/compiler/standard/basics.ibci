# IBCI Basic Syntax Fixture
# Responsibility: Primitive types, simple variable assignments, and basic function signatures.

int a = 10
str b = "hello"
float c = 3.14
bool d = True
none n = None

func add(int x, int y) -> int:
    return x + y

# Basic expressions
int result = add(a, 5)
str message = b
