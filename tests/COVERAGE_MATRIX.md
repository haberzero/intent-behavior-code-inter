# tests 覆盖矩阵（三段式引用：`file::class::method`）（三段式引用：`file::class::method`）

> 单一权威：逐域移植时填充；meta `test_matrix_sync.py` 机器校验（引用必须可解析）。
> 语义不变量编号（INV-*）与矩阵彻底完善（PT-TEST-3）在 Phase 4 一并收敛。

## 语义域（占位）

| 语义域 | 覆盖条目 |
|---|---|
| （移植中，待填充） | |
