# 架构 v2 健康回顾汇总（2026-09-11，供用户确认工作模式/目标/进度）

> 本文件 = 用户历次强调的原则 + Rust 内核/交互面健康审计发现 + R0-R4 落地状态 +
> 剩余问题清单的**汇总单点**。目的：用户确认"接下来的工作模式和目标"与"当前具体
> 进度"。设计阶段文档。

---

## 一、用户历次强调的原则（汇总，均具长期约束力）

| # | 原则 | 出处 |
|---|------|------|
| 1 | 停止 ⑦ 增量移植 → 内核接口层重审 + 测试体系重设计 + 授权推翻重来（**不计代价的底层清洁与重构，维护代码质量与架构健康**） | 2026-09-11 转向裁定 |
| 2 | 审计两问：有没有为对接测试定义的不干净接口代码？Rust/Python 分层职责分配是否合理？ | 转向裁定 |
| 3 | Rust 内核深扫四问：① 为复刻 Python 行为/保护不合理设计的纯兼容逻辑？② 不符合 Rust 设计原则的代码？③ 深层次架构问题？ | Rust 深扫 |
| 4 | 战略八点：① Python 保留=易用性（用户写 Python 实现自己的功能）② pip 安装 ③ tilelang/GPU 低级库 ④ Rust↔Python 协议化不能碎片 ⑤ 特性保留+允许更合理设计（无负向收益论证）⑥ 推倒重写执行核心+真多线程+统一化同构计算 ⑦ **内核易维护/易用/架构稳固 >> 语言性能**（AVX/GPU=战略储备非当前性能）⑧ 测试体系全量重构 | 战略框架 |
| 5 | 破坏性重构默认已授权（符合工程经验/普适性/架构正确即推进，详记决策）；大范围破坏性重构独立分支；禁 push；main 永不触碰 | AGENTS.md |
| 6 | IBCI 是注重易用性和领域专用性的 DSL，**绝不变成规则繁琐、严格、不易学习的 Rust 式通用语言**（内部严格性 ≠ 语言严格性） | 战略探讨 |
| 7 | **IBCI 职责 = 编排平台 + 统一表达**：不实现/不执行/不接管外部引擎内部（不执行 Python、不传 tilelang 源码、AVX 不进内核核心、内核 GIL-free）；数据形态统一 = Tensor；结构性统一 = 单一值模型 + 契约权威 + 多投影 | 职责重定位（本轮） |

## 二、Rust 内核健康审计发现与处置状态（使命 1 + 深扫）

| 审计发现 | 分类 | 处置状态 |
|----------|------|---------|
| 路由谓词堆（8 谓词 + 4 硬编码集合） | 碎片化 | ✅ R1-P1 能力声明表（ArtifactRouter 单一查询） |
| engine 镜像双路径（declared 白名单/旁路/特判/特化复刻） | 职责混淆 | ✅ R1-P2 StateMaterializer 单一物化，engine 零语义判断 |
| 错误跨边界字符串协议 + 正则 + 映射双真相 | 非类型化契约 | ✅ R1-P3 RustRuntimeError + 诊断码单一权威 |
| 内征集合双真相（手工清单 vs 分发表） | 双真相 | ✅ R1-P2 分发表派生 |
| WIP 会话 API（unsafe 全局注册表 + 双通道） | 测试驱动适配 | ✅ R1-P4 删除 + call_top_level_function + RustHostCallable |
| 静默降级系统性（13+ 实例：split() 无参/list 赋值 no-op/kb 治理门/…） | 正确性空洞 | ✅ R2-1 生产路径清零（13+ 实例）；kb/host 面 = GAP 登记（生产不可见） |
| f64 全包算术 + i64 有界（大整数精度静默丢失） | 数值契约冲突 | ✅ R2-3a typed i64 精确 + 溢出显式错误（DIVERGENCE bounded-int）；i128 = R2 续项 |
| stringly-typed 错误类名 | 不符合 Rust 哲学 | ✅ R2-4 ErrorKind enum（编译期拼写检查） |
| deserialize 失败静默空输出 | 静默兜底 | ✅ R2-2 fail-fast |
| 语义三重实现（Python 语义层/Rust 转录/执行器不消费） | 结构性 | ⏳ 执行器消费语义输出 = R2 续项/⑦ 终点（差分退场时收敛） |
| artifact UID 绑定 Python json.dumps（手写序列化器复刻） | 格式耦合 | ⏳ 语言级 IR + canonical 哈希 = 后续（值模型/IR 工作） |
| "Python 语义转录文化"（实证注释） | 权威源错位 | ⏳ 契约权威化 = 持续原则（type_inference 已示范"对齐公理"） |
| 方法分派 stringly-typed（call_method 大 match） | 不符合 Rust 哲学 | ⏳ 方法分派表 = R2 续项 |
| 两值模型跨边界（Python boxed vs Rust IbValue） | 结构性 | ✅ P2 typed 通道（值+标签）；Python IbObject 收敛 = ⑦ 终点 |

## 三、Rust↔Python 交互面健康（用户强调面）——R1/R4 成果

| 交互面 | 状态 |
|--------|------|
| 五协议（P1 能力声明 / P2 typed 值通道 / P3 typed 错误 / P4 宿主调用 / P5 计算基板） | P1-P4 ✅ 已落地；**P5 重定义为"计算编排协议"**（职责重定位） |
| HOST-EXT 用户 Python 扩展（点①） | ✅ R4-1（register_host_extension，typed 值转换 + 生态网关定位） |
| pip 安装（点②） | ✅ R4-2（maturin wheel，全新 venv 验收） |
| 内核 GIL-free（点⑥ GIL 关切） | ✅ 核心执行 allow_threads 释放；外部调用边界短暂取 GIL（编排本质） |
| 无碎片原则（点④） | ✅ P1-P4 消除谓词堆/字符串协议/会话双通道/双真相 |
| tilelang/GPU 接入（点③⑥⑦） | ⏳ R5：Tensor + 计算编排协议（外部引擎插件化，不传源码） |

## 四、测试体系健康（点⑧）——R3 状态

| 层 | 状态 |
|----|------|
| 语言行为层 tests/behavior/ | ✅ 52 用例（显式预期 + 诊断码断言面；34 语料升格） |
| 契约层 tests/contracts/ | ✅ 诊断码单一权威 + 错误面契约测试 |
| 宿主面层 tests/host/ | ✅ HOST-EXT + LLM 行为根测试 |
| 白盒降级 | ⏳ C-1 完成（首批 2 文件删除，契约显式承接）；~19 文件长尾待续 |
| 前端层/内核内部层（cargo test） | ⏳ 待建（R3 长尾） |
| 差分机制退场（阶段 D） | ⏳ ⑦ 终点（Python 参考内核退役时） |
| 全量基线 | 4333 passed / 0 failed / 1 skipped（~136s） |

## 五、当前进度与剩余工作（路线图）

### 已完成（unsafe-vibe-dev，全量 4333/0/1 零回归）
- **R0** 架构 v2 裁定 ✅ → **R1** 接口协议化（P1-P4）✅ merged → **R2** 执行核心
  硬化波 ✅ merged → **R3** 测试五层（A/B/C-1）✅ → **R4** HOST-EXT + pip ✅
- **职责重定位裁定** ✅（本文件 + `_ibci_role_platform.md`）

### 剩余（按优先级建议）
1. **R5 计算基板（重定义）**：Tensor 值入值模型（统一数据形态，vector → 1D Tensor
   演进）+ 计算编排协议（引擎注册/调度/缓冲交换，不做 SIMD 实现）。
2. **R3 阶段 C 长尾**：~19 个白盒文件逐项迁移（行为类重构为可观察断言 + 内部类
   删除，迁移映射表已立）；前端层/内核内部层（cargo test）组建。
3. **R2 续项**：方法分派表（enum 分派剩余）；i128 值模型（有界界扩大，边界/JSON
   复杂度——值模型核心工作）；执行器消费语义输出。
4. **R6（④层）Rust 插件协议**：ibci-sdk crate（值模型类型 + 注册 API）+ cdylib
   插件 ABI——回答"外部人员写 Rust"。
5. **阶段 D（⑦ 终点）**：差分机制退场 + Python 参考内核退役 + artifact IR 重设计
   （UID canonical 去 json.dumps 绑定）。

## 六、健康度评估（综合）

- **交互面**：从"8 谓词堆 + 字符串协议 + 会话双通道 + 双真相"→"五协议（P1-P4
  落地）+ HOST-EXT + 单一执行入口"——**架构级健康修复完成**。
- **内核执行**：从"静默降级 + f64 全包 + stringly-typed"→"无静默路径（生产面）+
  typed 数值 + ErrorKind"——**正确性/稳固性实质提升**（4306/2/1 → 4333/0/1）。
- **职责**：从"Python 转录 + 适配"→"编排平台 + 统一表达"（职责重定位裁定）——
  **方向性收敛完成**。
- **剩余** = 长尾迁移 + 计算编排 + 值模型深化 + 插件协议（均为增量/接口预留，
  非架构级风险）。
