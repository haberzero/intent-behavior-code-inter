# D2-70 边界：循环体内 LLM + llmexcept（KNOWN_LIMITS §十五：循环体/函数体内行为不可 dispatch，
# 走同步路径；无 llmexcept 保护则在赋值点抛 LLMParseError）
# doc: docs/KNOWN_LIMITS.md §十五 + 10_robustness.md §10.1（保护循环体内的行）
# 期望: 循环体内 LLM + llmexcept 正常收敛

list[str] items = ["3", "7", "2"]
for str item in items:
    int score = @~ $item 加 1 等于几？只答数字 ~
    llmexcept:
        retry "请只返回一个整数"
    print("score=" + (str)score)
print("DONE")
