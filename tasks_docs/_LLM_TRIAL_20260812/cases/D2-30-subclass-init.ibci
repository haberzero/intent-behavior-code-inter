# D2-30 多可能性：子类 auto-init 不含父类字段（KNOWN_LIMITS §六）
# doc: docs/KNOWN_LIMITS.md §六
# 期望: Dog auto-init 只接受 breed，d.name=None（记录实际）

class Animal:
    str name

class Dog(Animal):
    str breed

Dog d = Dog("Husky")
print("breed=" + d.breed)
print("name_null=" + (str)(d.name == None))
print("DONE")
