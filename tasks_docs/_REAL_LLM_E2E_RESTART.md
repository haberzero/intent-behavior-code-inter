# _REAL_LLM_E2E_RESTART — 真实 LLM 全面试用重启交接

> 2026-08-12 编制。**重启** 2026-08-11 被中断的"真实 LLM 使用 + 语法/语言功能评估"任务。
> 设计权威仍为 `_REAL_LLM_E2E_PLAN.md`；本文档 = 当前代码事实重估 + 重启范围界定 + 交接要点。

## 一、为什么需要重启（任务中断背景）

原 session（2026-08-11）执行了"真实 LLM e2e 全面试用 + 高强度批判检测"，产出
`_REAL_LLM_E2E_REPORT.md`。**但该报告基于含不合格操作（U1-U7）的旧代码**：

- `_REAL_LLM_E2E_REPORT.md` §六 自审明确："评估结论建立在含不合格操作的代码之上，不作为最终合并依据"。
- §四 的 **9/12 类特性试用表格在修复前执行**；其中意图机制结论（"效果弱=模型服从性"）已在后续复核中被
  **实证推翻**（实为 dispatch 路径 `@`/`@!` 从未注入 prompt 的**机制缺陷**，commit 7339220 已修复）。
- 之后发生的修复（U1-U7 根因修复、意图注入纠错、T1-T5 审计、PT-AUDIT-3、dispatch 观测修复 d6d28e1、
  run_batch 观测 df1a896）**均未在修复后代码上重新完整跑 12 类真实试用**。

**结论**：合并条件虽经工程维度重估"重新满足"（§七），但 **"真实 LLM 全语法特性试用 + 批判检测"
这一检测本身，在修复后代码上尚未完整重跑**——这是下一 session 必须重启的主线。

## 二、当前代码事实（2026-08-12 实核）

| 事实 | 状态 |
|------|------|
| 本地 LLM 端点 | ✅ `localhost:1234`（LM Studio，qwen3.6-35b-a3b 非思考）在线，`/v1/models` 可查 |
| 配置机制 | ✅ PT-FEAT-13 C1-C7 已完备：`project_root/api_config.json` 原生加载 + `providers/models/defaults` 分层 + `{env:VAR}` + `reasoning:false` |
| 真实 LLM examples 跑通 | ✅ 本 session 11 例全过（01 的意图冷酷极简实证 / 06 Enum+LLM / isolation / plugins） |
| 全量测试基线 | ✅ **2210 passed / 1 skipped** |
| 修复后需重验的语义点 | 见 §三 |

**examples 真实覆盖情况**（本 session 实核）：真实 LLM 调用仅 01（部分）/06 使用；04 是 mock 专用；
03 混 `MOCK:FALSE` 指令。**examples 跑通 ≠ 12 类全语法遍历**——前者是"示例代码端到端"，后者是
"逐语法特性用真实 LLM 各跑一遍 + 对抗批判"。

## 三、重启范围（修复后代码上的重验清单）

### A. 修复影响的重验点（必须在真实 LLM 下重跑，原结论已失效/被推翻）

| # | 特性 | 修复 commit | 重验要点 |
|---|------|------------|---------|
| A1 | 意图机制 `@`/`@!`（赋值 + 并行预调度路径） | 7339220 | 修复后真实模型实证意图**确实进入 prompt**（原"模型服从性低"误判已纠错）；赋值 + 表达式两条路径 + `@+` 持久意图对比 |
| A2 | 内建遮蔽 + LLM 表达式初始化（`int sum = @~...~`） | f58d525 | 真实 LLM 下 `int sum/@~` 遮蔽内建后正确赋值（示例 01 已跑通，需在遍历中独立确认） |
| A3 | `generator.to_list()` 用户显式调用 | 7260204（U1 正确架构） | 真实 LLM 生成器 + 显式 `.to_list()`/`.generic_next()`（原报告缺陷在修复前） |
| A4 | dispatch 赋值后 `idbg.current_llm()`/`ai.get_current_call_info()` | d6d28e1 | 批判检测可观测性：赋值后立即可查 prompt/意图（替代 monkeypatch 探针） |
| A5 | `ai.run_batch` 批路径观测 | df1a896 | 真实并发批量调用后可观测 + 结果正确 |

### B. 原报告未测项（低风险，需在重启时补正式记录）

| # | 特性 | 说明 |
|---|------|------|
| B1 | 动态宿主 `ihost.spawn_isolated`/`collect`/`run_isolated` | 隔离执行无 LLM 直接交互；本 session examples isolation_demo 已真实跑通——重启时记入报告即可 |
| B2 | 内建 `int()/str()/float()/len/zip` 等 | 纯确定性，MOCK 已充分覆盖；重启时选代表性内建真实组合验证 |
| B3 | 异常 `try/except/raise` | 控制流无 LLM 依赖；重启时与真实 LLM 失败场景组合验证 |

### C. 批判检测缺失场景（原报告 §五 未覆盖，重启重点）

| # | 场景 | 说明 |
|---|------|------|
| C1 | 长提示/复杂 `__to_prompt__` | 对象序列化进提示词的长提示边界（原报告未测） |
| C2 | 非确定性多次差异 | 同一输入多次结果差异是否被语言层正确处（尤其 bool/int 判断） |
| C3 | 超时/断连边界 | 本地端点慢/断连时行为（`ai.set_timeout`） |
| C4 | 并发真实调用扩展 | `run_batch` 多路 + 多任务（原只测 2 路） |

## 四、重启执行方式

1. **复用 `_REAL_LLM_E2E_PLAN.md` §二/§三**：端点已就绪（localhost:1234），配置已是一等机制。
   探针脚本素材在 `/tmp/opencode/llm_probe/*.ibci`（30 个，可参考改写）。
2. **§四 12 类全语法遍历**：按 `docs/SYNTAX_REFERENCE.md`/`docs/syntax/` 逐项，每项真实 LLM 跑一遍，
   重点执行 A1-A5 重验点。
3. **§五 批判检测**：格式服从 / llmexcept 收敛 / 意图注入（A1 实证）/ 长提示（C1）/ 非确定性（C2）/
   并发（C4）/ 超时（C3）。记录纪律：代码路径 + 复现脚本 + 期望 vs 实际 + 严重级别。
4. **B 类补正式记录**：ihost/内建/异常代表性验证。
5. **更新验证报告**：`_REAL_LLM_E2E_REPORT.md` §四表格基于**修复后代码**刷新；§七合并条件重估
   以"修复后 12 类完整试用 + 批判检测"为准（不再基于修复前的 9/12 推断）。

## 五、约束（硬原则）

- 全程本地 commit，**禁 push**（除非用户显式授权）。
- 破坏性重构按"独立分支 + 零风险直接合并/大风险 cherry-pick"政策；永远不触碰 main。
- 工作日志：所有自主决策/方案取舍/验证差异详尽记录于 WORKLOG。
- 新发现缺陷按 code-workflow Phase 0-5 根因修复；触及公理/语义/对外契约登记 PENDING_TASKS 待裁决。
- **真实 LLM 全面试用 + 批判检测通过后**，才可据此向用户报告"合并检测维度已基于修复后代码确认"，
  阶段 3 合并/push 仍须用户显式授权。

## 六、与当前文档的关系

| 文档 | 状态 |
|------|------|
| `_REAL_LLM_E2E_PLAN.md` | 设计权威，保持；执行顺序 §八 步骤 3-5 为本重启任务范围 |
| `_REAL_LLM_E2E_REPORT.md` | 需在重启后刷新 §四（12 类表格）/ §五（批判检测）/ §七（合并条件重估） |
| `_MERGE_READY_REPORT.md` | 本 session 产出；其中"真实 LLM e2e 检测通过"依据原报告，重启后需交叉核对更新 |
| `_MAIN_MERGE_PLAN.md` | 阶段 3 待用户授权；重启通过后即具备完整授权条件 |
| `_MERGE_READY_REPORT.md` 报告中的 examples 真实跑通 | 保留有效（本 session 实跑），但不等同 12 类遍历 |
