# D1-05 特化后方法参数类型检查（正/负）
# doc: docs/syntax/06_oop.md §6.4
# 期望: 类型不匹配编译报错；匹配通过
import ai
ai.set_mock_mode()
class Box[T]:
    T value
    func set(self, T v) -> void:
        self.value = v

Box[int] bi = Box[int](0)
bi.set(5)
print("set_int_ok=" + (str)bi.value)
print("DONE")
