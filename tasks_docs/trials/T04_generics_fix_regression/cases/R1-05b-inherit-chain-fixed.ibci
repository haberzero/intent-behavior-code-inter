# R1-05b G3 修复后适配：chain-aware auto-init 多参构造（历史 R1-05 为冻结证据）
# doc: docs/syntax/06_oop.md §6.4
# 期望输出: d1=A:5 / d2=6 / DONE
# 2026-08-13 新增：G3 修复（auto-init 合并继承链无默认值字段）后，Linked[int](data, tag)
# 一并绑定父类字段 data 与自身字段 tag；历史 R1-05 保持原始单参形态作为缺陷证据。
import ai
ai.set_mock_mode()
class Node[T]:
    T data
    Node[T] next = any
    func get(self) -> T:
        return self.data

class Linked[T](Node[T]):
    str tag
    func describe(self) -> str:
        return self.tag + ":" + (str)self.get()

Linked[int] l1 = Linked[int](5, "A")
Linked[int] l2 = Linked[int](6, "B")
l1.next = l2
print("d1=" + l1.describe())
print("d2=" + (str)l1.next.get())
print("DONE")
