# 批判：空 Optional 迭代诊断码一致性（修复后统一 RUN_ATTRIBUTE_ERROR，DOC-29 核销）
# doc: docs/architecture/03_type_system.md §8
# expect-class: PASS
# expect-code: RUN_ATTRIBUTE_ERROR
# expect-exit: nonzero
Optional[list[int]] e = None
for x in e:
    print(x)
