# Optional 值模型：Optional[list[int]] 有值包装后 len()/下标可用（OPTIONAL-CONTAINER-1 修复后核销）
# doc: docs/architecture/03_type_system.md §8
# expect-class: PASS
# expect-out: 3
# expect-exit: 0
Optional[list[int]] b = [1, 2, 3]
print(len(b))
