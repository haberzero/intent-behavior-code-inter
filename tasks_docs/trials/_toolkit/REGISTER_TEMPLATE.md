# REGISTER — <试用名>

> 生成方式：复制本模板，替换占位符。分类/级别/编号规范见 `_toolkit/CLASSIFICATION.md`。
> 日期 / 目标 / 参考规范版本（CLASSIFICATION.md 或 _toolkit 提交哈希）。

## 一、总览

- **用例总数**：<N> 个 + <M> 冒烟；<K> 次 harness 运行；全部经死循环保护。
- **PASS**：<n> 例；**GUARD**：<n> 例。
- **KERNEL_ISSUE**：<n> 项（编号列表）。**BOUNDARY**：<n> 项。**DOC_ISSUE**：<n> 项。
- **LIMIT**：<n> 项（含待修候选，见 §四）。**LLM_BEHAVIOR / HARNESS**：<n> 项。
- **冒烟验证**：死循环保护冒烟 <PASS/TIMEOUT-KILLED>。

## 二、缺陷登记（统一格式）

> 每条：编号 / 现象 / 证据（用例+日志）/ 根因分析起点或实证结论 / 修复状态 / commit。
> 编号规范：`KERNEL_ISSUE-<域>-<n>` / `BOUNDARY-<域>-<n>` / `DOC_ISSUE-<n>`。

### KERNEL_ISSUE-<域>-<n>（P<级别>）— <一句话现象>

- **现象**：<复现代码/输出>
- **证据**：`cases/<case>.ibci` + `logs/<case>.log`
- **根因分析起点**：<代码层线索，或实证结论>
- **修复状态**：待修复 / 已修复（commit）
- **登记**：PENDING_TASKS 对应行

### BOUNDARY-<域>-<n>（P<级别>）— <边界事实>

<同上，注明"非缺陷，需记录的设计事实/边界"及其文档化位置>

## 三、LIMIT / 待修候选池

> 命中文档化已知限制的用例登记在此，**并进待修候选池**（文档列出限制 ≠ 免罪）。
> 每条注明：用例 / 命中文档章节 / 是否值得修 / 评估结论。

## 四、逐例明细

| case_id | 目标 | 期望 | 实际 | 分类 | 级别 | 证据日志 |
|---------|------|------|------|------|------|----------|
| <D1-01> | <目标> | <期望> | <实际> | <分类> | <级别> | logs/<case>.log |

## 五、结论

<本试用地基验证了什么 / 发现什么 / 下一步>
