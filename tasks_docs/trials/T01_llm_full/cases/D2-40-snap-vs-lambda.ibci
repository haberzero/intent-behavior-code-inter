# D2-40 多层次：snapshot vs lambda 意图语义对比（定义时冻结 vs 调用时敏感）
# doc: docs/syntax/07_behavior_expressions.md §7.4 意图模式对比表
# 期望: snapshot 定义时冻结 @+；lambda 调用时用调用处意图；@! 只影响 lambda

# expect-class: PASS
# expect-out: DONE
# expect-exit: 0
# expect-llm: true
import ai
ai.load_project_config()
@+ 回复要极简，不超过8字
fn snap = snapshot -> str: @~ 描述天空 ~
fn lam = lambda -> str: @~ 描述天空 ~
@-

# snapshot 使用定义时冻结的意图（@+ 极简）→ 短回复
str s1 = snap()
# lambda 使用调用处意图（此时 @+ 已移除）→ 长回复
str l1 = lam()
print("snap_len=" + (str)s1.len())
print("lam_len=" + (str)l1.len())
print("DONE")
