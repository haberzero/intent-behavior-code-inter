# expect-class: PASS
# expect-llm: true
# expect-out: RED | False
import ai
ai.load_project_config()

class Color(Enum):
    str RED = "RED"
    str GREEN = "GREEN"
    str BLUE = "BLUE"

Color c = @~你是颜色枚举解析器。只输出一个成员名：RED、GREEN 或 BLUE，不要输出任何其他内容~
print((str)c)
print((str)(c == Color.GREEN))
