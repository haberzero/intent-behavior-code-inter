# 嵌套包同名类（a.sub.Box vs b.sub.Box）
# doc: docs/syntax/11_modules.md 包
# expect-class: PASS
# expect-out: from-a | from-b
# expect-exit: 0
import a.sub.box
import b.sub.box
print(a.sub.box.Box().who())
print(b.sub.box.Box().who())
