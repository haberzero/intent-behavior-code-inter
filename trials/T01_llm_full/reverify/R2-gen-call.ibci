# R2-005 I1 修复：生成器 __call__（for x in gc(n)）
# 期望: x=10 x=5
class GenCallable:
    func __call__(self, int n) -> int:
        yield 10
        yield n

GenCallable gc = GenCallable()
for int x in gc(5):
    print("x=" + (str)x)
print("DONE")
