# D3-60 KNOWN_LIMITS §十二：intent_context 类静态调用 push 静默无效 + SEM_INTENT_STATIC_CALL warning
# doc: docs/KNOWN_LIMITS.md §十二
# 期望: 静态调用不生效（记录实际 + warning 是否发出）

# expect-class: PASS
# expect-out: DONE
# expect-exit: 0
# expect-llm: true
import ai
ai.load_project_config()

intent_context.push("这条不会生效")
str r = @~ 描述天空 ~
print("r=" + r)
print("DONE")
