# D2-35 正交：ai.stream_call 流式调用（真实 LLM）
# doc: docs/syntax/11_modules.md §11.3
# 期望: 流式调用返回完整文本（真实 LLM）

# expect-class: PASS
# expect-out: DONE
# expect-exit: 0
# expect-llm: true
import ai
ai.load_project_config()

class 介绍:
    func __llm_call__(self) -> dict:
        return {"user_prompt": "用一句话介绍Python", "prompt_slots": [{"kind": "user_sys", "text": "你是助手"}]}
介绍 x = 介绍()
any h = ai.stream_call(x)
str full = await h
print("stream=" + full)
print("DONE")
