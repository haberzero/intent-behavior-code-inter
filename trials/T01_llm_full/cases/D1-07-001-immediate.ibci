# D1-07-001 即时行为 @~ 纯文本/str + $插值
# doc: docs/syntax/07_behavior_expressions.md §7.1
# 期望: 真实 LLM 返回字符串；$变量插值生效

# expect-class: PASS
# expect-out: DONE
# expect-exit: 0
# expect-llm: true
import ai
ai.load_project_config()
str name = "Alice"
str greeting = @~ 用 $name 打个招呼，中文 ~
print("greeting=" + greeting)

str joke = @~ 讲一个一句话程序员笑话，中文 ~
print("joke_len=" + (str)joke.len())
print("DONE")
