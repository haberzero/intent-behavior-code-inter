# T09-N3 LLM 函数第一等函数值（统一路径：fn 变量持有并调用）
# doc: docs/syntax/08_llm_functions.md + docs/syntax/05_functions.md
# expect-class: PASS
# expect-out: firstclass_ok=True | DONE
# expect-exit: 0
# expect-llm: true
import ai
ai.load_project_config()
llm 问数() -> int:
__sys__
你只返回一个整数，禁止任何其它内容。
__user__
返回 42
llmend

fn f = 问数
int v = f()
print("firstclass_ok=" + (str)(v == 42))
print("DONE")
