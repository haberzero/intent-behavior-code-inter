# T10-G1 __llm_call__ 返回非 dict（契约违约 fail-fast）
# doc: docs/syntax/08_llm_callable.md §8.1
# expect-class: GUARD
# expect-out: RuntimeError: VM: Call failed: invoke_llm_callable: __llm_call__ must return a config dict (got IbString).
# expect-exit: nonzero
# expect-llm: false
import ai
ai.set_mock_mode()
class 坏:
    func __llm_call__(self) -> str:
        return "not-a-dict"
坏 b = 坏()
str v = b()
print("DONE")
