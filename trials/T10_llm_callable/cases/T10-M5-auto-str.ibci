# T10-M5 未声明 expected_type 按字符串解析（auto 边界）
# doc: docs/syntax/08_llm_callable.md §8.5
# expect-class: PASS
# expect-out: auto=hi | DONE
# expect-exit: 0
# expect-llm: false
import ai
ai.set_mock_mode()
class 说话:
    func __llm_call__(self) -> dict:
        return {"user_prompt": "MOCK:STR:\"hi\""}
说话 t = 说话()
str v = t()
print("auto=" + v)
print("DONE")
