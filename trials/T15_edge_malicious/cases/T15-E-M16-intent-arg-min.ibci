# T15-E-M16 [边界清单 #9/运行时] llm 可调用类直接调用不传参（声明了参数）→ 缺参 fail-fast？（观测）
# doc: docs/syntax/08_llm_callable.md §8.2
# expect-class: GUARD
# expect-exit: nonzero
# expect-llm: false
import ai
ai.set_mock_mode()
class 需参:
    func __llm_call__(self, any a) -> dict:
        return {"user_prompt": "MOCK:STR:\"ok\""}
需参 s = 需参()
str v = s()
print("DONE")
