# T15-E-M13 [边界清单 #10] __retry__ max_retry=0 → fail-fast？（观测）
# doc: docs/syntax/08_llm_callable.md §8.4
# expect-class: GUARD
# expect-exit: nonzero
# expect-llm: false
import ai
ai.set_mock_mode()
class 坏:
    func __llm_call__(self) -> dict:
        return {"user_prompt": "MOCK:STR:\"ok\"", "expected_type": "int"}
    func __retry__(self) -> dict:
        return {"max_retry": 0}
坏 b = 坏()
int v = b()
print("DONE")
