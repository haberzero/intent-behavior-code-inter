# T15-E-M14 [边界清单 #13] 直接调用 f(args) 实参少于声明参数 → fail-fast？（观测）
# doc: docs/syntax/08_llm_callable.md §8.2
# expect-class: GUARD
# expect-exit: nonzero
# expect-llm: false
import ai
ai.set_mock_mode()
class 双参:
    func __llm_call__(self, any a, any b) -> dict:
        return {"user_prompt": "MOCK:STR:\"ok\""}
双参 s = 双参()
str v = s(1)
print("DONE")
