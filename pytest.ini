[pytest]
testpaths = tests
addopts = -q --tb=short --strict-markers
markers =
    slow: tests that are timing-sensitive or involve concurrency (may be flaky on CI)
    fast: pure unit tests with no I/O (< 200ms)
