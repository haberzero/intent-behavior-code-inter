# IBCI 语义覆盖矩阵 — 已迁移

> **本矩阵内容已迁移至 `tests/COVERAGE_MATRIX.md`（单一权威源）**。
> 新矩阵为**机器可校验的三段式引用**（`file::class::method`），由 `tests/meta/test_matrix_sync.py` 与
> `pytest --collect-only` nodeid 对账；13 个语义域 + INV-* 编号沿用，覆盖测试列全部为真实引用，
> 找不到语义吻合测试的行标 `🔶 缺失`（真缺口，不硬凑）。
>
> 本文件仅作迁移指针保留，不再维护内容（避免双源漂移）。
