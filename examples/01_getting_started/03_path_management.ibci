# 路径管理规则

import isys
import file

str entry_path = isys.entry_path()
str entry_dir = isys.entry_dir()
str project_root = isys.project_root()

file.write("./meta.txt", entry_path + "\n" + entry_dir)

if file.exists("./meta.txt"):
    file.remove("./meta.txt")
