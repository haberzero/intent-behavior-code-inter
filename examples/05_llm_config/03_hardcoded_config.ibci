import ai

# 情况 3：直接用内置组件进行硬编码配置（不推荐）
#
# 仅用于快速临时测试，不建议在生产或版本管理中使用。

print("--- [03_hardcoded_config.ibci] ---")

# 直接硬编码参数
# ai.set_config("https://api.openai.com/v1", "sk-xxx", "gpt-4")

# 模拟测试配置 (TESTONLY)
ai.set_config("TESTONLY", "TESTONLY", "TESTONLY")

print("已完成硬编码配置（示例中使用 TESTONLY 代替）")

str res = @~硬编码测试~
print("AI 回复: " + res)
